"""Settings for loggers.
Attributes:
    LOG_SETTINGS (dict): Settings for logging.
        Keys:
            debug (bool): A flag to determine if you want to print the input and outputs of a function.
            inspect_class (bool): A flag to determine if we want to inspect the class variables.
"""

LOG_SETTINGS = {"debug": True,
"inspect_class": False}